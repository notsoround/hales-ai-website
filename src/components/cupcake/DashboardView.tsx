import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { ArrowLeft, ArrowUpRight, ChevronRight, Headphones, Loader2, RefreshCw } from 'lucide-react';
import './dashboard.css';
import './meal-detail.css';
import { WeightHistoryConnected as WeightHistory } from './WeightHistoryConnected';
import { loadWeightPage, type WeightRecord } from './weightApi';
import { weightTime } from './weightHistoryModel';
import { entriesForDay, entriesForType, interventionGroups, metricScales, rangeLabel, transactionAmount, transactionEntriesForCurrency } from './dashboardModel';

export type DashboardRange = { preset: 'day' | 'week' | 'month' | 'custom' | 'all'; start: string; end: string; timezone: 'America/Chicago' };
export type DashboardEntry = { id: string; type: string; date: string; time?: string; title: string; body?: string; amount?: number | null; currency?: string | null; calories?: number | null; parentId?: string | null; source?: string; icon?: string; details?: { mealId?: string | null; outcome?: unknown; boughtAnyway?: unknown; [key: string]: unknown } };
export type FoodMeal = { id: string; status: string; createdAt?: string; description?: string; total?: Record<string, number | null>; warnings?: string[]; lookupWarnings?: string[]; totalIsPartial?: boolean; items?: Array<{ id: string; name: string; quantity?: number | null; unit?: string | null; nutrition?: Record<string, number | null>; provenance?: string; calculation?: string; confidence?: string; variantWarning?: string; sourceCitations?: Array<{ title: string; url: string }> }>; needsClarification?: boolean; clarification?: string | null; sheetLoggedAt?: string | null };
export type DashboardStats = { range: { start: string; end: string; timezone?: string; coverage?: string; spendSource?: string }; totals: { calories: number; spend: number; currency?: string; weightLb?: number | null; weightDate?: string }; series: Array<{ date: string; calories: number; spend: number }>; entries: DashboardEntry[]; nextCursor?: string | null; totalCount?: number; outcomes?: {wins:number;losses:number;missed:number;unscored:number;interventions:number}; coverage?: string };
export type DashboardLoader = (range: DashboardRange, cursor?: string) => Promise<DashboardStats>;
export type InterventionUpdate = (entry: DashboardEntry, note: string, boughtAnyway: boolean | null) => Promise<void>;
export type TalkAboutEntry = (entry: DashboardEntry) => void;

const chicago = 'America/Chicago' as const;
const dayMs = 86400000;
function chicagoToday() { return new Intl.DateTimeFormat('en-CA', { timeZone: chicago, year: 'numeric', month: '2-digit', day: '2-digit' }).format(new Date()); }
function shift(date: string, days: number) { const d = new Date(`${date}T12:00:00Z`); d.setUTCDate(d.getUTCDate() + days); return d.toISOString().slice(0, 10); }
function startFor(preset: DashboardRange['preset'], end: string) { return preset === 'day' ? end : shift(end, preset === 'week' ? -6 : -29); }
function parseMoney(body = '') { const match = body.match(/[-+]?\$\s*([\d,]+(?:\.\d+)?)/); return match ? Number(match[1].replace(/,/g, '')) : null; }
function fallbackStats(items: Array<Record<string, unknown>>, range: DashboardRange): DashboardStats {
  const start = range.start, end = range.end;
  const entries = items.map((item, index) => {
    const body = String(item.body || ''); const type = String(item.type || 'signal');
    const calorieMatch = body.match(/([\d,]+)\s*kcal/i);
    return { id: String(item.id || `${type}-${item.ts || index}`), type, date: String(item.date || ''), time: String(item.time || ''), title: String(item.title || 'Signal'), body, amount: type === 'transaction' ? parseMoney(body) : null, currency: type === 'transaction' && body.trim().startsWith('$') ? 'USD' : null, calories: type === 'food' && calorieMatch ? Number(calorieMatch[1].replace(/,/g, '')) : null, icon: String(item.icon || '•') };
  }).filter(item => item.date >= start && item.date <= end);
  const seen = new Set<string>();
  const unique = entries.filter(entry => { const key = `${entry.type}|${entry.date}|${entry.title}|${entry.amount ?? ''}`; if (entry.type === 'transaction' && seen.has(key)) return false; seen.add(key); return true; });
  const series = Array.from({ length: Math.max(1, Math.round((Date.parse(`${end}T00:00:00Z`) - Date.parse(`${start}T00:00:00Z`)) / dayMs) + 1) }, (_, i) => { const date = shift(start, i); const day = unique.filter(item => item.date === date); return { date, calories: day.reduce((sum, item) => sum + (item.calories || 0), 0), spend: day.reduce((sum, item) => sum + (item.amount || 0), 0) }; });
  return { range: { start, end, timezone: chicago, coverage: 'Recent feed coverage only; this feed may omit older entries.' }, totals: { calories: unique.reduce((sum, item) => sum + (item.calories || 0), 0), spend: unique.reduce((sum, item) => sum + (item.amount || 0), 0), weightLb: unique.find(item => item.type === 'activity')?.amount ?? null }, series, entries: unique, coverage: 'Recent feed coverage only; this feed may omit older entries.' };
}

export function FeedDashboard({ loadStats, feedLoader, loadMeal, updateIntervention, onTalkAbout }: { loadStats?: DashboardLoader; feedLoader?: () => Promise<{ items: Array<Record<string, unknown>> }>; loadMeal?: (mealId: string) => Promise<FoodMeal>; updateIntervention?: InterventionUpdate; onTalkAbout?: TalkAboutEntry }) {
  const today = chicagoToday();
  const [latestWeight,setLatestWeight]=useState<WeightRecord|null>(null);const [weightError,setWeightError]=useState(false);
  useEffect(()=>{let live=true;void loadWeightPage().then(p=>{if(live)setLatestWeight(p.latestMeasurement);}).catch(()=>{if(live)setWeightError(true);});return()=>{live=false;};},[]);
  const [preset, setPreset] = useState<DashboardRange['preset']>('week');
  const [customEnd, setCustomEnd] = useState(today); const [customStart, setCustomStart] = useState(shift(today, -6));
  const [stats, setStats] = useState<DashboardStats | null>(null);
  const [selected, setSelected] = useState<DashboardEntry | null>(null);
  const [selectedList, setSelectedList] = useState<{ title: string; entries: DashboardEntry[] } | null>(null);
  const [pageBusy,setPageBusy]=useState(false);
  const [weightOpen, setWeightOpen] = useState(false);
  const [visibleCount, setVisibleCount] = useState(20);
  const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const requestVersion=useRef(0);
  const range = useMemo<DashboardRange>(() => ({ preset, start: preset === 'custom' ? customStart : startFor(preset, today), end: preset === 'custom' ? customEnd : today, timezone: chicago }), [preset, today, customEnd, customStart]);
  const refresh = useCallback(async () => { const version=++requestVersion.current; setLoading(true); setStats(null); setError(''); try { const result = loadStats ? await loadStats(range) : fallbackStats((await (feedLoader ? feedLoader() : Promise.resolve({ items: [] }))).items, range); if(version===requestVersion.current)setStats(result); } catch (e) { if(version===requestVersion.current)setError(e instanceof Error ? e.message : 'Could not load dashboard data.'); } finally { if(version===requestVersion.current)setLoading(false); } }, [feedLoader, loadStats, range]);
  useEffect(() => { setVisibleCount(20); setSelected(null); setSelectedList(null); void refresh(); }, [refresh]);
  const loadMore=async()=>{if(pageBusy||!stats?.nextCursor||!loadStats)return;const version=requestVersion.current;setPageBusy(true);try{const page=await loadStats(range,stats.nextCursor);if(version===requestVersion.current)setStats(current=>current?{...current,nextCursor:page.nextCursor,entries:[...current.entries,...page.entries.filter(e=>!current.entries.some(old=>old.id===e.id))]}:current);}catch(e){setError(e instanceof Error?e.message:'Could not load more.');}finally{setPageBusy(false);}};
  if (selected) return <SignalDetail backLabel={weightOpen ? 'Back to weight history' : undefined} entry={selected} onBack={() => setSelected(null)} loadMeal={loadMeal} updateIntervention={updateIntervention} onUpdated={() => { setSelected(null); void refresh(); }} onTalkAbout={onTalkAbout} />;
  if (weightOpen) return <WeightHistory entries={stats?.entries || []} range={range} loading={loading} error={error} hasMore={!!stats?.nextCursor} loadingMore={pageBusy} onMore={() => void loadMore()} onBack={() => setWeightOpen(false)} onRefresh={() => void refresh()} onRange={(p,start,end) => { setPreset(p); if(start)setCustomStart(start); if(end)setCustomEnd(end); }} onSelect={setSelected} />;
  if (selectedList) { const groups=interventionGroups(stats?.entries||[]); const groupKey=({'Times you won':'wins','Times you caved':'losses','Missed calls':'missed','Interventions':'interventions','Unscored interventions':'unscored'} as const)[selectedList.title as 'Times you won']; return <SignalList title={selectedList.title} entries={groupKey?groups[groupKey]:selectedList.entries} hasMore={!!stats?.nextCursor} loadingMore={pageBusy} onMore={() => void loadMore()} onBack={() => setSelectedList(null)} onSelect={setSelected} />; }
  const scales = metricScales(stats?.series || []);
  const moneyPrefix = stats?.totals.currency && !['USD','$'].includes(stats.totals.currency) ? `${stats.totals.currency} ` : '$';
  return <section className="cc-dashboard" aria-label="Cupcake dashboard">
    <div className="cc-heading-row"><div><span className="cc-eyebrow">YOUR DASHBOARD</span><h2>Wins, losses &amp;<br /><em>the bigger picture.</em></h2></div><button className="cc-secondary" aria-label="Refresh dashboard" onClick={() => void refresh()} disabled={loading}><RefreshCw className={loading ? 'cc-spin' : ''} size={18} /></button></div>
    <div className="cc-dashboard-range" role="tablist" aria-label="Dashboard range">{(['day', 'week', 'month', 'custom', 'all'] as const).map(option => <button key={option} role="tab" aria-selected={preset === option} onClick={() => setPreset(option)}>{rangeLabel(option)}</button>)}</div>
    {preset === 'custom' && <div className="cc-dashboard-dates"><label>From<input type="date" value={customStart} max={customEnd} onChange={event => setCustomStart(event.target.value)} /></label><label>Through<input type="date" value={customEnd} onChange={event => setCustomEnd(event.target.value)} /></label></div>}
    {error && <p className="cc-error" role="alert">{error}</p>}
    {loading && !stats ? <p className="cc-thinking"><Loader2 className="cc-spin" size={17} />Loading your signals…</p> : stats && <>
      <h3>Your wins &amp; losses</h3>
      <p className="cc-muted cc-small">Tap a total to see its entries. Choose Last 30 days or Custom to find older outcomes.</p>
      {(() => { const outcomes = interventionGroups(stats.entries); const counts=stats.outcomes||{wins:outcomes.wins.length,losses:outcomes.losses.length,missed:outcomes.missed.length,unscored:outcomes.unscored.length,interventions:outcomes.interventions.length}; return <div className="cc-dashboard-cards cc-outcome-cards" aria-label="Recorded intervention outcomes">
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Times you won', entries: outcomes.wins })}><strong>{counts.wins}</strong><span>Times you won · recorded stops</span></button>
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Times you caved', entries: outcomes.losses })}><strong>{counts.losses}</strong><span>Times you caved · bought anyway</span></button>
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Missed calls', entries: outcomes.missed })}><strong>{counts.missed}</strong><span>Tried to call · no answer</span></button>
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Interventions', entries: outcomes.interventions })}><strong>{counts.interventions}</strong><span>Interventions · all recorded calls</span></button>
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Unscored interventions', entries: outcomes.unscored })}><strong>{counts.unscored}</strong><span>Unscored · needs review</span></button>
        <p className="cc-muted cc-small cc-outcome-note">Wins, losses, and missed calls come from the phone bot log. Last 7 days is empty if Cupcake has not called you in that week — use Last 30 or Custom (from August 1) for the older scorecard. Leaving a note on a missed call is how you tell Cupcake what happened after you did not pick up.</p>
      </div>; })()}
      <div className="cc-card"><h3>Momentum</h3>{(()=>{const g=interventionGroups(stats.entries),c=stats.outcomes||{wins:g.wins.length,losses:g.losses.length,missed:g.missed.length,unscored:g.unscored.length};const total=c.wins+c.losses;return <><strong>{total?`${Math.round(c.wins/total*100)}% recorded wins`:'Not enough scored outcomes yet'}</strong><p className="cc-muted">{c.wins} recorded wins / {total} scored outcomes. {c.missed+c.unscored} missed or unscored entries excluded. An agreement to stop is not verified follow-through.</p></>})()}</div><div className="cc-card"><h3>Data coverage</h3><p className="cc-muted">{stats.totalCount??stats.entries.length} available records · {stats.entries.length} loaded. This describes the app’s evidence, not your health or behavior.</p>{stats.coverage&&<p className="cc-muted cc-small">{stats.coverage}</p>}</div>
      {preset!=='all'&&<div className="cc-dashboard-cards">
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: 'Food entries', entries: entriesForType(stats.entries, 'food') })}><strong>{Math.round(stats.totals.calories).toLocaleString()}</strong><span>Est. calories logged · View food</span></button>
        <button className="cc-dashboard-card" onClick={() => setSelectedList({ title: `${stats.totals.currency || 'USD'} transactions`, entries: transactionEntriesForCurrency(stats.entries, stats.totals.currency) })}><strong>{moneyPrefix}{stats.totals.spend.toFixed(2)}</strong><span>{stats.range.spendSource === 'Plaid posted outflows' ? 'Posted spend' : 'Logged spend'} · View transactions</span></button>
        {(() => { const latest=latestWeight?{lb:latestWeight.weightLb,at:Date.parse(latestWeight.measuredAt)}:null; return <button className="cc-dashboard-card" onClick={() => setWeightOpen(true)}><strong>{latest ? `${latest.lb.toFixed(1)} lb` : '—'}</strong><span>{latest ? `Measured ${weightTime(latest.at)} · View graph` : weightError?'Weight unavailable · Open to retry':'Weight history · Awaiting a timestamped weigh-in'}</span></button>; })()}
      </div>
      }
      {preset==='all'&&<button className="cc-dashboard-card" onClick={()=>setWeightOpen(true)}>Weight history · Open graph</button>}
      {preset!=='all'&&<div className="cc-dashboard-chart cc-card">
        <div className="cc-heading-row"><h3>{rangeLabel(preset)}</h3><span className="cc-muted cc-small">{stats.range.start} → {stats.range.end}</span></div>
        <div className="cc-chart-scales"><span>Calories · max {Math.round(scales.calorieMax).toLocaleString()} kcal</span><span>Spend · max {moneyPrefix}{scales.spendMax.toFixed(2)}</span></div>
        <div className="cc-bars" style={{ '--cc-days': stats.series.length } as React.CSSProperties} aria-label="Calories and spend by day">{stats.series.map((day, index) => <button key={day.date} className="cc-bar-day" aria-label={`${day.date}: ${day.calories} calories, ${moneyPrefix}${day.spend.toFixed(2)} spend. Open all entries.`} onClick={() => setSelectedList({ title: day.date, entries: entriesForDay(stats.entries, day.date) })}><span className="cc-bar-stack"><i style={{ height: `${scales.caloriePercent(day.calories)}%` }} /><b style={{ height: `${scales.spendPercent(day.spend)}%` }} /></span><small className={stats.series.length > 14 && index % 5 !== 0 && index !== stats.series.length - 1 ? 'cc-visually-hidden' : ''}>{day.date.slice(5)}</small></button>)}</div>
        <div className="cc-chart-legend"><span><i />Calories (kcal scale)</span><span><b />Spend ({stats.totals.currency || 'USD'} scale)</span></div>
      </div>
      }
      <div className="cc-dashboard-section"><div className="cc-heading-row"><h3>Signals in this range</h3><span className="cc-muted cc-small">{stats.entries.length} total</span></div>{stats.entries.slice(0, visibleCount).map(entry => <button className="cc-dashboard-entry" key={entry.id} onClick={() => setSelected(entry)}><span className="cc-dashboard-entry-icon">{entry.icon || '•'}</span><span><strong>{entry.title}</strong><small>{entry.date}{entry.time ? ` · ${entry.time}` : ''} · {entry.body || entry.type}</small></span><ChevronRight size={17} /></button>)}{visibleCount < stats.entries.length && <button className="cc-secondary cc-dashboard-more" onClick={() => setVisibleCount(count => count + 20)}>Show more ({stats.entries.length - visibleCount} remaining)</button>}{!stats.entries.length && <p className="cc-muted">No entries in this range.</p>}</div>
      {stats.nextCursor&&<button className="cc-secondary" disabled={pageBusy} onClick={()=>void loadMore()}>{pageBusy?'Loading…':'Load older records'}</button>}
      {stats.coverage && <p className="cc-muted cc-small cc-dashboard-coverage">{stats.coverage}</p>}
    </>}
  </section>;
}

function SignalList({ title, entries, onBack, onSelect,hasMore,onMore,loadingMore }: { title: string; entries: DashboardEntry[]; onBack: () => void; onSelect: (entry: DashboardEntry) => void;hasMore?:boolean;onMore?:()=>void;loadingMore?:boolean }) {
  const [visibleCount, setVisibleCount] = useState(20);
  return <section className="cc-dashboard cc-dashboard-detail"><button className="cc-text-button" onClick={onBack}><ArrowLeft size={17} />Back to dashboard</button><span className="cc-eyebrow">DASHBOARD ENTRIES</span><h2>{title}</h2>{hasMore&&<button className="cc-secondary" disabled={loadingMore} onClick={onMore}>{loadingMore?'Loading…':'Load older records'}</button>}<p className="cc-muted">{entries.length} {entries.length === 1 ? 'entry' : 'entries'}</p><div className="cc-dashboard-section">{entries.slice(0, visibleCount).map(entry => <button className="cc-dashboard-entry" key={entry.id} onClick={() => onSelect(entry)}><span className="cc-dashboard-entry-icon">{entry.icon || '•'}</span><span><strong>{entry.title}</strong><small>{entry.date}{entry.time ? ` · ${entry.time}` : ''} · {entry.body || entry.type}</small></span><ChevronRight size={17} /></button>)}{!entries.length && <p className="cc-muted">No entries in this selection.</p>}{visibleCount < entries.length && <button className="cc-secondary cc-dashboard-more" onClick={() => setVisibleCount(count => count + 20)}>Show more ({entries.length - visibleCount} remaining)</button>}</div></section>;
}

export function SignalDetail({ entry, onBack, backLabel = 'Back to dashboard', loadMeal, updateIntervention, onUpdated, onTalkAbout }: { entry: DashboardEntry; onBack: () => void; backLabel?: string; loadMeal?: (mealId: string) => Promise<FoodMeal>; updateIntervention?: InterventionUpdate; onUpdated?: () => void; onTalkAbout?: TalkAboutEntry }) {
  const mealId = entry.details?.mealId;
  const [meal, setMeal] = useState<FoodMeal | null>(null); const [mealError, setMealError] = useState('');
  const [note, setNote] = useState(''); const [bought, setBought] = useState<'yes' | 'no' | ''>('');
  const [saving, setSaving] = useState(false); const [saveError, setSaveError] = useState('');
  useEffect(() => { if (!mealId || !loadMeal) return; let current = true; void loadMeal(mealId).then(value => { if (current) setMeal(value); }).catch(error => { if (current) setMealError(error instanceof Error ? error.message : 'Meal details unavailable.'); }); return () => { current = false; }; }, [loadMeal, mealId]);
  const mealWarnings = meal ? [...(meal.lookupWarnings || []), ...(meal.warnings || [])] : [];
  const canUpdate = entry.type === 'intervention' && !!updateIntervention;
  const ownerUpdated = entry.details?.ownerUpdated === true;
  const missed = entry.details?.missed === true || /no[_ -]?answer|missed|voicemail/i.test(String(entry.details?.outcome || ''));
  return <section className="cc-dashboard cc-dashboard-detail"><button className="cc-text-button" onClick={onBack}><ArrowLeft size={17} />{backLabel}</button><span className="cc-eyebrow">SIGNAL DETAIL</span><h2>{entry.icon || '•'} {entry.title}</h2><p className="cc-muted">{entry.date}{entry.time ? ` · ${entry.time}` : ''} · {entry.type}</p><div className="cc-card"><p className="cc-answer-text">{entry.body || 'No additional detail was provided.'}</p>{missed && <p className="cc-muted">Cupcake tried this call and did not get an answer.{ownerUpdated ? ' You later left an update.' : ' Leave a note below if you want the log to show that you followed up.'}</p>}{typeof entry.details?.summary === 'string' && entry.details.summary ? <p className="cc-muted">{entry.details.summary}</p> : null}{entry.amount != null && entry.type !== 'activity' && <strong>{transactionAmount(entry.amount, entry.currency)}</strong>}{entry.type === 'activity' && entry.amount != null && <><strong>{entry.amount} lb</strong><p className="cc-muted">{entry.details?.weightKind==='measurement' ? `Scale measurement · ${String(entry.details.measuredAt || 'Time unavailable')}` : entry.details?.weightKind==='aggregate' ? 'Aggregate average; not an individual weigh-in.' : 'Measurement type or original time is unverified.'}</p></>}{entry.calories != null && <strong>{entry.calories.toLocaleString()} kcal</strong>}{entry.type==='food'&&!mealId&&<p className="cc-muted">This older entry has no saved item breakdown.{typeof entry.details?.notes==='string'&&entry.details.notes ? ` Original notes: ${entry.details.notes}` : ''}</p>}{entry.source && <a className="cc-text-button" href={entry.source} target="_blank" rel="noreferrer">Open source <ArrowUpRight size={15} /></a>}{mealId && loadMeal && !meal && !mealError && <p className="cc-thinking"><Loader2 className="cc-spin" size={16} />Loading meal breakdown…</p>}{mealError && <p className="cc-muted">{mealError} This historical entry has no saved breakdown.</p>}{mealWarnings.map(warning => <p className="cc-muted" key={warning}>Estimate warning: {warning}</p>)}{meal && <div className="cc-meal-breakdown"><strong>Saved meal total: {meal.total?.calories == null ? 'Calories unknown' : `${meal.total.calories} kcal${meal.totalIsPartial ? ' (partial)' : ''}`}</strong>{meal.totalIsPartial && <small>Some items could not be identified, so this total is incomplete.</small>}{meal.items?.map(item => <div key={item.id}><span>{item.name}{item.quantity != null && item.quantity !== 1 ? ` × ${item.quantity}${item.unit ? ` ${item.unit}` : ''}` : ''}</span><small>{item.nutrition?.calories == null ? 'Calories unknown' : `${item.nutrition.calories} kcal`} · {item.provenance || 'Estimate provenance unavailable'}</small>{item.calculation && <small>Calculation: {item.calculation}</small>}{item.variantWarning && <small>Estimate warning: {item.variantWarning}</small>}{item.sourceCitations?.length ? <small className="cc-meal-sources">Sources: {item.sourceCitations.map((source, index) => <span key={`${source.url}-${source.title}`}>{index > 0 && ', '}<a href={source.url} target="_blank" rel="noreferrer">{source.title}<ArrowUpRight size={11} /></a></span>)}</small> : <small>Source link unavailable</small>}</div>)}</div>}{entry.type === 'intervention' && onTalkAbout && <button type="button" className="cc-primary" onClick={() => onTalkAbout(entry)}><Headphones size={18}/>Talk to Cupcake about this call</button>}{canUpdate && <form className="cc-intervention-note" onSubmit={event => { event.preventDefault(); if (!note.trim() || saving || !updateIntervention) return; setSaving(true); setSaveError(''); void updateIntervention(entry, note.trim(), bought === 'yes' ? true : bought === 'no' ? false : null).then(() => onUpdated?.()).catch(error => setSaveError(error instanceof Error ? error.message : 'Could not save that update.')).finally(() => setSaving(false)); }}><label className="cc-label">What happened after the call<textarea value={note} onChange={event => setNote(event.target.value)} maxLength={500} rows={3} required placeholder="I missed it. I put it back / I bought it anyway." /></label><fieldset className="cc-bought"><legend>Did you buy it anyway?</legend><label><input type="radio" name="boughtAnyway" value="no" checked={bought==='no'} onChange={() => setBought('no')} /> No</label><label><input type="radio" name="boughtAnyway" value="yes" checked={bought==='yes'} onChange={() => setBought('yes')} /> Yes</label><label><input type="radio" name="boughtAnyway" value="" checked={bought===''} onChange={() => setBought('')} /> Not sure</label></fieldset>{saveError && <p className="cc-error" role="alert">{saveError}</p>}<button className="cc-primary" disabled={saving || !note.trim()}>{saving ? 'Saving…' : 'Update Cupcake'}</button></form>}</div></section>;
}
