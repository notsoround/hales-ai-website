import { ArrowLeft, ArrowUpRight } from 'lucide-react';
import './experience/experience.css';
export function GetStarted({onNavigate}:{onNavigate:(page:'home')=>void}){
 return <main className="hx" style={{minHeight:'100vh'}}><section className="hx-section"><button className="hx-text-button" onClick={()=>onNavigate('home')}><ArrowLeft size={18}/> Back to Hales.ai</button><p className="hx-eyebrow" style={{marginTop:45}}>FROM PROBLEM TO WORKING SYSTEM</p><h1 style={{fontSize:'clamp(48px,6vw,86px)',margin:'25px 0 45px'}}>Start with the work.<br/><em>Build what matters.</em></h1><div className="hx-work-grid">{[{title:'Find the friction.',text:'Bring one process that costs your team time: missed calls, scattered information, or a repetitive handoff.'},{title:'Map a useful first version.',text:'We define the people, data, integrations, and permissions involved, along with a clear way to judge success.'},{title:'Build, learn, and improve.',text:'Start with a focused implementation. Test realistic cases, keep human review where it matters, and improve from actual use.'}].map((s,i)=><article className="hx-work-card" key={s.title} style={{minHeight:300}}><p className="hx-eyebrow">0{i+1}</p><h2 style={{fontSize:36,margin:'30px 0 20px'}}>{s.title}</h2><p>{s.text}</p></article>)}</div><a className="hx-button" style={{marginTop:35}} href="/contact-us">Tell us what you’re building <ArrowUpRight size={18}/></a></section></main>;
}
